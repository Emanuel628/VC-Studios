# VITE_API_URL is required for a production frontend deployment (e.g. Vercel).
# Set it to the deployed backend's full HTTPS URL (the Railway service, not this frontend).
# Leave it unset for local development - vite.config.ts proxies /api to the local backend instead.
VITE_API_URL=
