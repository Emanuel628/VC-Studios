# Baseline Status

No accepted crawler baseline is committed yet.

Run the GitHub workflow or `python crawl.py crawl`, inspect every snapshot and manually verify the authenticated dashboard flows, then run `python crawl.py accept`. Only reviewed baseline snapshots should be committed.
